#include "NRendererDevice.h"

#include <vector>

// Find a graphics adapter we can use.
// Return true if we have found a compatable adapter.
//bool NRenderDevice::fetchGraphicsAdapter()
//{
//	if (FAILED(CreateDXGIFactory1(__uuidof(IDXGIFactory1), (void**)deviceDatabase.get()))) 
//	{
//		// Devices could not be enumerated 
//		return false;
//	}
//
//	for (int i = 0; deviceDatabase->EnumAdapters1(i, &(device.get())); ++i) 
//	{
//		
//	}
//
//	return false;
//}
